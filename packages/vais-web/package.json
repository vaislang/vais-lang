{
  "name": "vais-web",
  "private": true,
  "version": "0.1.0",
  "description": "VaisX/VaisKit - Token-efficient frontend framework",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
