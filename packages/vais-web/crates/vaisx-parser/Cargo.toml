[package]
name = "vaisx-parser"
description = ".vaisx file parser — block separation, template/style/script parsing, AST generation"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
